import React from "react";

import CreateForm from "../../finalFormsamples";
import { FORM_TYPE_EXTERNAL_VALIDATORS_FINAL_FORM } from "../../constants";

let StepFour = (props) => {
    return (
        <>
            <CreateForm  formType={FORM_TYPE_EXTERNAL_VALIDATORS_FINAL_FORM} /> 
        </>
    );
};

export default StepFour;
