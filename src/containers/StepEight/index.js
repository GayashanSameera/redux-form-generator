import React from "react";

import CreateForm from "../../finalFormsamples";
import { FORM_TYPE_FORM_SECTION_FINAL_FORM } from "../../constants";

let StepEight = (props) => {
    return (
        <>
            <CreateForm  formType={FORM_TYPE_FORM_SECTION_FINAL_FORM} /> 
        </>
    );
};

export default StepEight;
